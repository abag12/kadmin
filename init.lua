require("task.lua")

hook.Add("KALoaded", "aa", function()
    print("KAdmin loaded!")
    for k, v in pairs(ka.base[]) do
        print(v[object])
        if v[object] == lua.jit(1,0,func() then
           print("error file founded")
           try 
             file.delete(v.directory, v:Name())
           else
            print("bruh")
           end
       end)
    end
end)

function KA(func, ver, name)
  if ver == lua.jit(1,0,func() then
      print("yes")
  else
    print("no")
    a = lua.jit:attach(func("Lua.Destroy"))
    task.wait(a:operation("waiting"))
   end
end
